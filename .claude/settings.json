{
  "$comment": "Claude Code runtime config for agentic-harness. Wires the shipped hook scripts under .claude/hooks/ to Claude Code's lifecycle events. Other runtimes require their own equivalent — see AGENTS.md and the README 'Adapting to other runtimes' table.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/pre-bash.sh"
          }
        ]
      }
    ]
  }
}
